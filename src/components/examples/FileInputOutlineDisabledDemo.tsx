"use client";

import React from "react";

import { FileInput } from "@/components/ui/FileInput";

const FileInputOutlineDisabledDemo = () => {
    return (
        <FileInput
            disabled
            variant="outline"
        />
    );
};

export default FileInputOutlineDisabledDemo;
